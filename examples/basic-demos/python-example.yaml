# Python CLI Example
package_name: demo-python-cli
command_name: demo_py
display_name: "Demo Python CLI"
description: "A sample Python CLI built with Goobits"
version: "1.0.0"
language: python
author: "Demo User"
email: "demo@example.com"
license: "MIT"
cli_output_path: "src/demo_cli/cli.py"

cli:
  name: demo_py
  tagline: "Python CLI demonstration"
  commands:
    greet:
      desc: "Greet someone with style"
      args:
        - name: name
          desc: "Name to greet"
          required: true
        - name: message
          desc: "Custom greeting message"
          required: false
          default: "Hello"
      options:
        - name: style
          short: s
          type: str
          desc: "Greeting style"
          choices: ["formal", "casual", "excited"]
          default: "casual"
        - name: count
          short: c
          type: int
          desc: "Repeat greeting N times"
          default: 1
        - name: uppercase
          short: u
          type: flag
          desc: "Convert to uppercase"
          default: false
        - name: language
          short: l
          type: str
          desc: "Language code"
          choices: ["en", "es", "fr"]
          default: "en"
    
    info:
      desc: "Display system and environment information"
      options:
        - name: format
          short: f
          type: str
          desc: "Output format"
          choices: ["text", "json", "yaml"]
          default: "text"
        - name: verbose
          short: v
          type: flag
          desc: "Show detailed information"
          default: false
        - name: sections
          short: s
          type: str
          desc: "Comma-separated sections to show"
          default: "all"