#!/usr/bin/env python3
"""
Minimal Test CLI - A minimal test CLI for validation
Generated by Goobits CLI Framework
"""

import click
import sys
from typing import Any
try:
    from .hooks import *  # Import all hook functions
except ImportError:
    # Hooks not implemented yet
    pass
@click.group(
    name="Minimal Test CLI",
    help="Basic CLI for validation testing",
    context_settings={"help_option_names": ["-h", "--help"]}
)
@click.version_option(version="1.0.0")
def cli():
    """Basic CLI for validation testing"""
    pass
@cli.command(name="hello", help="Say hello to someone")@click.option(
    "--name",
    help="Name to greet",    default='World',)
def hello_command(name):
    """Say hello to someone"""
    try:
        # Call the hook function if it exists
        hook_name = "on_hello"
        if hook_name in globals():
            hook_func = globals()[hook_name]
            hook_func(name)
        else:
            click.echo(f"Hook function '{hook_name}' not implemented")
            sys.exit(1)
    except Exception as e:
        click.echo(f"Error: {e}", err=True)
        sys.exit(1)
if __name__ == "__main__":
    cli()