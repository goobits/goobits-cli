# Basic Goobits CLI Configuration
package_name: test_typescript_cli
command_name: test_typescript_cli
display_name: "Test_Typescript_Cli"
description: "A CLI tool built with Goobits"

# Language selection (python, nodejs, or typescript)
language: typescript

python:
  minimum_version: "3.8"

dependencies:
  required:
    - pipx

cli:
  name: test_typescript_cli
  tagline: "Your awesome CLI tool"
  commands:
    hello:
      desc: "Say hello"
      is_default: true
      args:
        - name: name
          desc: "Name to greet"
          required: false
      options:
        - name: greeting
          short: g
          desc: "Custom greeting"
          default: "Hello"
