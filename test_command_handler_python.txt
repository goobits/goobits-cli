#!/usr/bin/env python3
"""
Test Universal CLI - Test CLI for universal template validation
Generated by Goobits CLI Framework
"""

import click
import sys
from typing import Any
try:
    from .hooks import *  # Import all hook functions
except ImportError:
    # Hooks not implemented yet
    pass
@click.group(
    name="test-cli",
    help="None",
    context_settings={"help_option_names": ["-h", "--help"]}
)
@click.version_option(version="1.0.0")
def cli():
    """None"""
    pass
@cli.command(name="greet", help="Greet someone")@click.argument("name")@click.option(
    "--loud", "-l",
    help="Greet loudly",    default=False,)
def greet_command(name, loud):
    """Greet someone"""
    try:
        # Call the hook function if it exists
        hook_name = "on_greet"
        if hook_name in globals():
            hook_func = globals()[hook_name]
            hook_func(name, loud)
        else:
            click.echo(f"Hook function '{hook_name}' not implemented")
            sys.exit(1)...