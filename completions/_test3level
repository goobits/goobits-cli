#compdef test3level
# Zsh completion script for Test 3-Level Commands (test3level)
# Auto-generated by goobits-cli

# Main completion function
_test3level() {
    local context state state_descr line
    typeset -A opt_args

    # Try dynamic completion first
    if _test3level_dynamic; then
        return 0
    fi

    # Fallback to static completion
    _test3level_static
}

# Dynamic completion using CLI's built-in completion
_test3level_dynamic() {
    local cli_bin="test3level"
    
    # Check if CLI supports completion
    if ! command -v "$cli_bin" >/dev/null 2>&1; then
        return 1
    fi
    
    # Get completion suggestions from the CLI itself
    local suggestions
    suggestions=(${(f)"$("$cli_bin" _completion zsh "$words" "$#BUFFER" 2>/dev/null)"})
    
    if [[ $#suggestions -gt 0 ]]; then
        compadd -a suggestions
        return 0
    fi
    
    return 1
}

# Static completion as fallback
_test3level_static() {
    local context state state_descr line
    typeset -A opt_args

    _arguments -C \
        '(-h --help)'{-h,--help}'[Show help information]' \
        '(-V --version)'{-V,--version}'[Show version information]' \
        '1: :_test3level_commands' \
        '*::arg:->args' && return 0

    case $state in
        args)
            case $words[1] in
                simple)
                    _test3level_simple
                    ;;
                api)
                    _test3level_api_subcommands
                    ;;
                nested)
                    _test3level_nested_subcommands
                    ;;
                plugin)
                    _test3level_plugin
                    ;;
                upgrade)
                    _test3level_upgrade
                    ;;
            esac
            ;;
    esac
}

# Command completion
_test3level_commands() {
    local commands=(
        'simple:Simple command'
        'api:API management'
        'nested:Nested command test'
        'upgrade:Upgrade Test 3-Level Commands to the latest version'
        'plugin:🔌 Manage plugins for Test 3-Level Commands'
        'help:Show help information'
    )
    _describe 'commands' commands
}

# simple command completion
_test3level_simple() {
    _arguments \
        '(-h --help)'{-h,--help}'[Show help for simple command]' \
        ':message:Message to display' \
}
# api subcommands completion
_test3level_api_subcommands() {
    local subcommands=(
        'v1:API v1 endpoints'
    )
    
    if [[ $CURRENT -eq 2 ]]; then
        _describe 'api subcommands' subcommands
    else
        case $words[2] in
            v1)
                _arguments \
                    '(-h --help)'{-h,--help}'[Show help for v1 subcommand]' \
                ;;
        esac
    fi
}
# nested subcommands completion
_test3level_nested_subcommands() {
    local subcommands=(
        'level2:Level 2 subcommand'
    )
    
    if [[ $CURRENT -eq 2 ]]; then
        _describe 'nested subcommands' subcommands
    else
        case $words[2] in
            level2)
                _arguments \
                    '(-h --help)'{-h,--help}'[Show help for level2 subcommand]' \
                ;;
        esac
    fi
}

# Plugin command completion
_test3level_plugin() {
    local plugin_commands=(
        'list:List all available and loaded plugins'
        'install:Install a plugin from npm'
        'uninstall:Uninstall a plugin'
        'create:Create a new plugin template'
        'info:Show detailed information about a plugin'
    )
    
    if [[ $CURRENT -eq 2 ]]; then
        _describe 'plugin commands' plugin_commands
    else
        case $words[2] in
            list)
                _arguments \
                    '--json[Output as JSON]' \
                    '(-h --help)'{-h,--help}'[Show help for plugin list]'
                ;;
            install)
                _arguments \
                    '(-g --global)'{-g,--global}'[Install globally]' \
                    '--version[Install specific version]:version:' \
                    '(-h --help)'{-h,--help}'[Show help for plugin install]' \
                    ':plugin-name:'
                ;;
            uninstall|remove)
                _arguments \
                    '(-g --global)'{-g,--global}'[Uninstall globally]' \
                    '(-h --help)'{-h,--help}'[Show help for plugin uninstall]' \
                    ':plugin-name:'
                ;;
            create)
                _arguments \
                    '--type[Plugin type]:type:(function class npm)' \
                    '--dir[Output directory]:directory:_directories' \
                    '(-h --help)'{-h,--help}'[Show help for plugin create]' \
                    ':plugin-name:'
                ;;
            info)
                _arguments \
                    '(-h --help)'{-h,--help}'[Show help for plugin info]' \
                    ':plugin-name:'
                ;;
        esac
    fi
}

# Upgrade command completion
_test3level_upgrade() {
    _arguments \
        '--check[Check for updates without installing]' \
        '--version[Install specific version]:version:' \
        '--pre[Include pre-release versions]' \
        '--dry-run[Show what would be done without doing it]' \
        '(-h --help)'{-h,--help}'[Show help for upgrade command]'
}

# Custom completion functions for specific option types

# Load the completion
_test3level "$@"

# Enable completion caching for better performance
zstyle ':completion:*:*:test3level:*' cache-policy _test3level_cache_policy
_test3level_cache_policy() {
    # Cache completions for 1 hour
    [[ -n "$1" && "$1" -ot "$(date -v-1H)" ]]
}

# Add color support
zstyle ':completion:*:*:test3level:*:descriptions' format '%F{green}-- %d --%f'
zstyle ':completion:*:*:test3level:*:commands' group-name commands
zstyle ':completion:*:*:test3level:*:options' group-name options