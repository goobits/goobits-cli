{# Universal Progress Manager Template - Framework Integration
   
   This template generates progress indicators using the extracted ProgressFramework.
   Provides consistent user experience with spinners, progress bars, and status messages.
   
   Variables expected:
   - language: Target language (python, nodejs, typescript, rust)
   - project: Project metadata
#}

{%- set progress_framework = get_progress_framework() -%}

{# Generate language-specific progress management code using the framework #}
{{ progress_framework.generate_language_code(language, {'project': project}) }}