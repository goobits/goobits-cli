{# Completion Manager Template - Simplified via Framework Extraction #}
{%- set completion_framework = get_completion_framework() -%}
{{ completion_framework.generate_completion_code(language, {
    'project': project,
    'cli': cli,
    'completion': completion | default({}),
    'commands': cli.commands | default({})
}) }}