{# Universal Setup Script Template - Framework Integration
   
   This template generates setup scripts using the extracted SetupFramework.
   Provides project installation and configuration across different languages.
   
   Variables expected:
   - language: Target language (python, nodejs, typescript, rust)
   - project: Project metadata
   - cli: CLI schema
#}

{%- set setup_framework = get_setup_framework() -%}

{# Generate language-specific setup script using the framework #}
{{ setup_framework.generate_setup_script(language, {
    'project': project,
    'cli': cli,
    'package_name': project.package_name,
    'command_name': project.command_name,
    'display_name': project.display_name
}) }}