{# Universal Hook System Template (Phase 2.3 Extracted)
   
   This template generates the hook system integration using the extracted
   HookFramework. The original 912-line template logic has been moved to
   Python frameworks for better testability and maintainability.
   
   Variables expected:
   - language: Target language
   - project: Project metadata
   - cli: CLI schema with commands
#}
{%- set hook_framework = get_hook_framework() -%}
{%- set config_dict = {
    'project': project,
    'cli': cli,
    'language': language,
    'development': development | default({}),
    'performance': performance | default({}),
    'error_handling': error_handling | default({}),
    'template_format': template_format | default('default')
} -%}
{{- hook_framework.generate_hook_code(language, config_dict) -}}