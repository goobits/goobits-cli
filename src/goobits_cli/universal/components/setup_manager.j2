{# Universal Setup Manager Template - Framework Integration
   
   This template generates setup/installation scripts using the extracted SetupFramework.
   Provides robust installation, upgrade, and dependency management across platforms.
   
   Variables expected:
   - language: Target language (python, nodejs, typescript, rust)
   - project: Project metadata
   - installation: Installation configuration
#}

{%- set setup_framework = get_setup_framework() -%}

{# Generate language-specific setup script using the framework #}
{{ setup_framework.generate_setup_script(language, {
    'project': project,
    'installation': installation,
    'package_name': installation.pypi_name or project.name,
    'command_name': project.name,
    'python_version': installation.python_version or '3.8'
}) }}