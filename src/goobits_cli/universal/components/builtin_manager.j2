{# Universal Built-in Manager Template - Framework Version
   
   This template generates built-in commands using the BuiltinFramework.
   Replaces 967 lines of duplicated logic with framework integration.
   
   Variables expected:
   - language: Target language (python, nodejs, typescript, rust)
   - project: Project metadata
   - installation: Installation configuration
#}

{# Get the builtin framework instance #}
{%- set builtin_framework = get_builtin_framework() -%}

{# Generate builtin command code using framework #}
{{ builtin_framework.generate_builtin_code(language, {
    'project': project,
    'installation': installation,
    'features': features | default({}),
    'config_filename': config_filename | default('goobits.yaml'),
    'generator_version': generator_version | default('3.0.1')
}) }}