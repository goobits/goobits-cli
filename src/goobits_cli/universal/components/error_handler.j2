{# Universal Error Handler Template (Phase 3.2 Extracted)
   
   This template generates error handling patterns using the extracted
   ErrorFramework. The original 684-line template logic has been moved to
   Python frameworks for better testability and maintainability.
   
   Variables expected:
   - language: Target language
   - project: Project metadata
   - error_types: Optional custom error type definitions
#}
{%- set error_framework = get_error_framework() -%}
{%- set config_dict = {
    'project': project,
    'language': language,
    'error_handling': error_handling | default({}),
    'custom_errors': error_types | default([])
} -%}
{{- error_framework.generate_error_code(language, config_dict) -}}