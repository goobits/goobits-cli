{# REPL Loop Template - Simplified via Framework Extraction #}
{%- set repl_framework = get_repl_framework() -%}
{{ repl_framework.generate_repl_code(language, {
    'project': project,
    'cli': cli,
    'features': features | default({}),
    'commands': cli.commands | default({})
}) }}