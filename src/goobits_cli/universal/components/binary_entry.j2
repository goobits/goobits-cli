#!/usr/bin/env node
{# Binary entry point for TypeScript CLI #}
{% if language == "typescript" %}
/**
 * Binary entry point for {{ project.name }}
 * Generated by Goobits CLI Framework
 */

// Use ts-node for development, compiled JS for production
if (process.env.NODE_ENV === 'development') {
  require('ts-node').register();
  require('../cli.ts');
} else {
  require('../dist/cli.js');
}
{% endif %}