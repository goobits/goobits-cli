{# Universal README Template
   
   This template generates README.md files for projects,
   providing documentation and usage instructions.
   
   Variables expected:
   - language: Target language
   - project: Project metadata
   - cli: CLI schema with commands
#}

# {{ project.display_name | default(project.package_name) }}

{{ project.description | default('A CLI application generated by Goobits CLI Framework') }}

## Installation

{%- if language == 'rust' %}

### From Source

```bash
# Clone the repository
git clone {{ project.repository | default('https://github.com/user/' + project.package_name) }}
cd {{ project.package_name }}

# Build and install
cargo build --release
cargo install --path .
```

### Using Cargo

```bash
cargo install {{ project.package_name }}
```

{%- elif language == 'nodejs' %}

### Using npm

```bash
npm install -g {{ project.package_name }}
```

### Using yarn

```bash
yarn global add {{ project.package_name }}
```

{%- elif language == 'typescript' %}

### Using npm

```bash
npm install -g {{ project.package_name }}
```

### Using yarn

```bash
yarn global add {{ project.package_name }}
```

{%- elif language == 'python' %}

### Using pip

```bash
pip install {{ project.package_name }}
```

### Using pipx (recommended)

```bash
pipx install {{ project.package_name }}
```

{%- endif %}

## Usage

{{ project.command_name }} is a command-line tool with the following commands:

{% if cli.commands -%}
{%- for command_name, command in cli.commands.items() %}
### `{{ project.command_name }} {{ command_name }}`

{{ command.desc | default('No description provided') }}

{%- if command.args or command.options %}

**Usage:**
```bash
{{ project.command_name }} {{ command_name }}{% if command.args %} {% for arg in command.args %}{% if not arg.required %}[{% endif %}<{{ arg.name }}>{% if not arg.required %}]{% endif %}{% if not loop.last %} {% endif %}{% endfor %}{% endif %}{% if command.options %} [OPTIONS]{% endif %}
```

{%- if command.args %}

**Arguments:**
{% for arg in command.args -%}
- `{{ arg.name }}`: {{ arg.desc | default('No description provided') }}{% if not arg.required %} (optional){% endif %}
{% endfor %}
{%- endif %}

{%- if command.options %}

**Options:**
{% for option in command.options -%}
- `--{{ option.name }}`{% if option.short %}, `-{{ option.short }}`{% endif %}: {{ option.desc | default('No description provided') }}{% if option.default %} (default: {{ option.default }}){% endif %}
{% endfor %}
{%- endif %}

{%- endif %}

{%- endfor %}
{%- endif %}

### Global Options

- `--help`, `-h`: Show help message
- `--version`, `-V`: Show version information
{%- if language == 'rust' or language == 'python' %}
- `--verbose`, `-v`: Enable verbose output
{%- endif %}

## Examples

{%- if cli.commands %}
{%- for command_name, command in cli.commands.items() %}

```bash
# {{ command.desc | default('Example usage') }}
{{ project.command_name }} {{ command_name }}{% if command.args %} {% for arg in command.args %}example_{{ arg.name }}{% if not loop.last %} {% endif %}{% endfor %}{% endif %}
```
{%- endfor %}
{%- endif %}

## Development

{%- if language == 'rust' %}

### Building

```bash
cargo build
```

### Testing

```bash
cargo test
```

### Running

```bash
cargo run -- <command> [args...]
```

{%- elif language == 'nodejs' or language == 'typescript' %}

### Setup

```bash
npm install
```

### Building{% if language == 'typescript' %} (TypeScript)

```bash
npm run build
```
{% endif %}

### Testing

```bash
npm test
```

### Running

```bash
npm start <command> [args...]
```

{%- elif language == 'python' %}

### Setup

```bash
pip install -e .
```

### Testing

```bash
pytest
```

### Running

```bash
python -m {{ project.package_name | replace('-', '_') }} <command> [args...]
```

{%- endif %}

## License

{{ project.license | default('MIT') }}

{% if project.author -%}
## Author

{{ project.author }}{% if project.email %} <{{ project.email }}>{% endif %}
{%- endif %}

---

Generated by [Goobits CLI Framework](https://github.com/DataBassGit/goobits)