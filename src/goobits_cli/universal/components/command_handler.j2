{# Universal Command Handler Template - Framework Version
   
   This template generates CLI command structure using the CommandFramework.
   Replaces 1,464 lines of duplicated logic with framework integration.
   
   Variables expected:
   - language: Target language (python, nodejs, typescript, rust)
   - project: Project metadata
   - cli: CLI schema with commands, arguments, options
#}

{# Get the command framework instance #}
{%- set command_framework = get_command_framework() -%}

{# Generate CLI command code using framework #}
{{ command_framework.generate_command_code(language, {
    'project': project,
    'cli': cli,
    'features': features | default({}),
    'config_filename': config_filename | default('goobits.yaml'),
    'generator_version': generator_version | default('3.0.1'),
    'datetime': datetime
}) }}