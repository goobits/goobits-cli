# {{ display_name }}

{{ description }}

{% if cli.tagline %}
> {{ cli.tagline }}
{% endif %}

## Installation

### From Package Manager (Recommended)

{% if language == 'python' %}
```bash
pipx install {{ installation.pypi_name | default(package_name) }}
```
{% elif language in ['nodejs', 'typescript'] %}
```bash
npm install -g {{ installation.npm_name | default(package_name) }}
```
{% elif language == 'rust' %}
```bash
cargo install {{ package_name }}
```
{% endif %}

### From Source

Clone this repository and build from source:

```bash
git clone {{ repository | default('https://github.com/user/repo') }}
cd {{ package_name }}
./setup.sh --dev
```

### Development Installation

For development with live updates:

```bash
./setup.sh --dev
```

## Usage

### Basic Commands

{% for cmd_name, cmd_data in cli.commands.items() %}
{% if not cmd_data.subcommands %}
#### `{{ command_name }} {{ cmd_name }}`{% if cmd_data.icon %} {{ cmd_data.icon }}{% endif %}

{{ cmd_data.desc }}

```bash
{{ command_name }} {{ cmd_name }}{% if cmd_data.args %}{% for arg in cmd_data.args %}{% if arg.required %} <{{ arg.name }}>{% else %} [{{ arg.name }}]{% endif %}{% endfor %}{% endif %}{% if cmd_data.options %} [OPTIONS]{% endif %}
```

{% if cmd_data.args %}
**Arguments:**
{% for arg in cmd_data.args %}
- `{{ arg.name }}`: {{ arg.desc }}{% if arg.choices %} (choices: {{ arg.choices | join(', ') }}){% endif %}
{% endfor %}
{% endif %}

{% if cmd_data.options %}
**Options:**
{% for opt in cmd_data.options %}
- `{% if opt.short %}-{{ opt.short }}, {% endif %}--{{ opt.name }}{% if opt.type != "flag" %} <{{ opt.type }}>{% endif %}`: {{ opt.desc }}{% if opt.default %} (default: {{ opt.default }}){% endif %}
{% endfor %}
{% endif %}

{% else %}
#### `{{ command_name }} {{ cmd_name }}`{% if cmd_data.icon %} {{ cmd_data.icon }}{% endif %}

{{ cmd_data.desc }}

**Subcommands:**
{% for subcmd_name, subcmd_data in cmd_data.subcommands.items() %}
- `{{ subcmd_name }}`: {{ subcmd_data.desc }}
{% endfor %}
{% endif %}
{% endfor %}

### Global Options

{% if cli.options %}
{% for option in cli.options %}
- `{% if option.short %}-{{ option.short }}, {% endif %}--{{ option.name }}{% if option.type != "flag" %} <{{ option.type }}>{% endif %}`: {{ option.desc }}{% if option.default %} (default: {{ option.default }}){% endif %}
{% endfor %}
{% endif %}

### Examples

```bash
# Show help
{{ command_name }} --help

# Show version
{{ command_name }} --version

{% for cmd_name, cmd_data in cli.commands.items() %}
{% if not cmd_data.subcommands and cmd_data.args %}
# Example {{ cmd_name }} command
{{ command_name }} {{ cmd_name }}{% for arg in cmd_data.args %}{% if arg.required %} "example_{{ arg.name }}"{% endif %}{% endfor %}

{% endif %}
{% endfor %}
```

## Configuration

{% if language == 'python' %}
The CLI stores configuration in `~/.config/{{ package_name }}/config.yaml`.
{% elif language in ['nodejs', 'typescript'] %}
Configuration locations:

- **Linux**: `~/.config/{{ package_name }}/`
- **macOS**: `~/Library/Application Support/{{ package_name }}/`
- **Windows**: `%APPDATA%\{{ package_name }}\`
{% elif language == 'rust' %}
The CLI stores configuration in `~/.config/{{ package_name }}/config.yaml`.
{% endif %}

You can edit this file directly or use the CLI to manage settings.

### Configuration Options

- `settings.auto_update`: Enable automatic updates (default: false)
- `settings.log_level`: Set logging level (debug, info, warn, error)
- `features.colored_output`: Enable colored terminal output (default: true)
- `features.progress_bars`: Show progress bars for long operations (default: true)

## Development

### Building

{% if language == 'python' %}
```bash
# Install development dependencies
pip install -e .[dev]

# Build package
python -m build
```
{% elif language in ['nodejs', 'typescript'] %}
```bash
# Install dependencies
npm install

# Build (if TypeScript)
npm run build

# Run tests
npm test
```
{% elif language == 'rust' %}
```bash
# Debug build
cargo build

# Release build
cargo build --release
```
{% endif %}

### Testing

{% if language == 'python' %}
```bash
# Run all tests
pytest

# Run with coverage
pytest --cov={{ package_name }}
```
{% elif language in ['nodejs', 'typescript'] %}
```bash
# Run all tests
npm test

# Run with coverage
npm run test:coverage
```
{% elif language == 'rust' %}
```bash
# Run all tests
cargo test

# Run tests with output
cargo test -- --nocapture
```
{% endif %}

### Running

{% if language == 'python' %}
```bash
# Run from source
python -m {{ package_name | replace('-', '_') }} --help
```
{% elif language in ['nodejs', 'typescript'] %}
```bash
# Run from source (development)
node cli.js --help

# Or using npm link
npm link && {{ command_name }} --help
```
{% elif language == 'rust' %}
```bash
# Run from source (debug)
cargo run -- --help

# Run specific command
cargo run -- {{ cli.commands.keys() | list | first }} --help
```
{% endif %}

## Shell Completions

{% if completion_support | default(true) %}
Generate shell completions for better command-line experience:

```bash
./setup.sh --completions
```

This creates completion files in the `completions/` directory for:
- Bash
- Zsh  
- Fish

### Installing Completions

**Bash:**
```bash
source completions/{{ command_name }}.bash
```

**Zsh:**
```bash
# Add to your ~/.zshrc
fpath=(./completions $fpath)
autoload -U compinit && compinit
```

**Fish:**
```bash
cp completions/{{ command_name }}.fish ~/.config/fish/completions/
```
{% endif %}

## Architecture

This CLI is built using:

{% if language == 'python' %}
- **[Click](https://click.palletsprojects.com/)**: Command-line interface creation toolkit
- **[Rich](https://rich.readthedocs.io/)**: Rich text and beautiful formatting
- **[Pydantic](https://pydantic-docs.helpmanual.io/)**: Data validation using type hints
{% elif language == 'nodejs' %}
- **[Commander.js](https://github.com/tj/commander.js/)**: Complete solution for command-line interfaces
- **[Inquirer.js](https://github.com/SBoudrias/Inquirer.js/)**: Interactive command-line prompts
- **[Chalk](https://github.com/chalk/chalk)**: Terminal string styling
{% elif language == 'typescript' %}
- **[Commander.js](https://github.com/tj/commander.js/)**: Complete solution for command-line interfaces
- **[TypeScript](https://www.typescriptlang.org/)**: Typed superset of JavaScript
- **[Inquirer.js](https://github.com/SBoudrias/Inquirer.js/)**: Interactive command-line prompts
{% elif language == 'rust' %}
- **[Clap](https://docs.rs/clap/)**: Command-line argument parsing with derive macros
- **[Anyhow](https://docs.rs/anyhow/)**: Flexible error handling
- **[Serde](https://docs.rs/serde/)**: Serialization/deserialization for configuration
- **[Tokio](https://docs.rs/tokio/)**: Async runtime (optional feature)
{% endif %}

### Project Structure

{% if language == 'python' %}
```
src/
├── {{ package_name | replace('-', '_') }}/
│   ├── __init__.py      # Package initialization
│   ├── cli.py           # CLI entry point
│   ├── config.py        # Configuration management
│   ├── commands.py      # Command implementations
│   └── utils.py         # Utility functions
└── app_hooks.py         # User-defined business logic
```
{% elif language in ['nodejs', 'typescript'] %}
```
├── cli.{% if language == 'typescript' %}ts{% else %}js{% endif %}           # CLI entry point
├── package.json         # NPM package configuration
├── src/
│   ├── hooks.{% if language == 'typescript' %}ts{% else %}js{% endif %}       # User-defined business logic
│   ├── config.{% if language == 'typescript' %}ts{% else %}js{% endif %}      # Configuration management
│   └── utils.{% if language == 'typescript' %}ts{% else %}js{% endif %}       # Utility functions
└── completions/         # Shell completion scripts
```
{% elif language == 'rust' %}
```
src/
├── main.rs          # CLI entry point and command definitions
├── lib.rs           # Library exports and core functionality  
├── config.rs        # Configuration management
├── commands.rs      # Command implementations
├── hooks.rs         # User-defined business logic
└── utils.rs         # Utility functions
```
{% endif %}

## Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/amazing-feature`)
3. Make your changes
4. Add tests for new functionality
5. Run tests and ensure they pass
6. Commit your changes (`git commit -am 'Add amazing feature'`)
7. Push to the branch (`git push origin feature/amazing-feature`)
8. Open a Pull Request

## License

This project is licensed under the {{ license | default('MIT') }} License - see the [LICENSE](LICENSE) file for details.

## Changelog

### {{ version | default('1.0.0') }}
- Initial release
- Core CLI functionality implemented
{% for cmd_name, cmd_data in cli.commands.items() %}
- `{{ cmd_name }}` command: {{ cmd_data.desc }}
{% endfor %}