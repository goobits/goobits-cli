suite_name: completion-commands
description: Completion parity is currently out-of-contract for default runners

setup:
  goobits_config: ../test-cli-config.yaml
  cleanup: true

tests:
  - name: completion_parity_pending
    description: Completion parity deferred until cross-language command contract is stabilized
    command: --help
    skip_languages: ["all"]
    skip_reason: "Completion parity is deferred (see PARITY_CONTRACT.md)"
