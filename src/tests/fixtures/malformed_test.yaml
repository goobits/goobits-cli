name: test-cli
version: 1.0.0
language: python

cli:
  name: testcli
  commands:
    - name: hello
      description: Say hello
      options:
        - name: --name
          description: Name to greet
          # Missing closing bracket on list
          type: string
    - name: goodbye
      # Invalid indentation below
     description: Say goodbye
      options:
        - name: --message
          type: string
          required: yes  # Should be boolean true/false, not yes