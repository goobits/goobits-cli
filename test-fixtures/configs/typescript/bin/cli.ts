#!/usr/bin/env node
/**
 * Binary entry point for Test TypeScript CLI
 * Generated by Goobits CLI Framework
 */

// Use ts-node for development, compiled JS for production
if (process.env.NODE_ENV === 'development') {
  const { register } = await import('ts-node');
  register();
  await import('../cli.ts');
} else {
  await import('../dist/cli.js');
}
