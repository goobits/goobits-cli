package_name: "test-node-cli"
command_name: "testnodecli"
display_name: "TestNodeCLI"
description: "Node.js test CLI for validation"
language: nodejs

python:
  minimum_version: "3.8"

dependencies:
  core: []

installation:
  pypi_name: "test-node-cli"

cli:
  name: "TestNodeCLI"
  tagline: "Node.js test CLI for validation"
  version: "1.0.0"
  commands:
    hello:
      desc: "Say hello"
      args:
        - name: name
          desc: "Name to greet"
          required: true
    test:
      desc: "Run a test"